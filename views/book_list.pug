extends layout

block content
  h1=title
  p List of all books in the LocalLibrary

  if book_list.length 
    ul  
      each book in book_list
        li 
          a(href=book.url) #{book.title}
          |  #{book.author.name} 
  else
    p Books not found...